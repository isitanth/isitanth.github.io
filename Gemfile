source "https://rubygems.org"

gem "jekyll", "~> 3.9"
gem "kramdown-parser-gfm"

# Pinned: the latest ffi (>= 1.17) requires Ruby >= 3.0, which breaks macOS system Ruby 2.6.
gem "ffi", "< 1.17"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
